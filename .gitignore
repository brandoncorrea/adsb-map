
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Editor
.idea/

# Aircraft enrichment database — third-party DATA fetched by `bb db:fetch`
# (tar1090-db / Mictronics). Large and license-unstated: never committed; the
# app degrades to absent when it is missing. See README 'Aircraft enrichment
# data'.
resources/public/db/

# Build output
.cpcache/
.shadow-cljs/
node_modules/
resources/public/js/
# The stylesheet is compiled from src/css by Garden (`bb css`), exactly as the
# js is compiled from src/cljs by shadow. Source of truth: adsb.css.app.
resources/public/app.css
target/
.clj-kondo/.cache/
.nrepl-port

# Secrets
.env
